---
description: 工具箱（webtools）项目框架与开发约定
alwaysApply: true
---

# 工具箱（webtools）框架要求

## 项目信息

- **产品名**：工具箱
- **GitHub 仓库**：`https://github.com/cnzhanglu/webtools.git`
- **GitHub 用户名**：`cnzhanglu`
- **部署**：Cloudflare Pages，Build output = `/`，无构建命令
- **Cloudflare Pages 项目名建议**：`webtools`

## 技术约束（必须遵守）

- 纯静态前端：HTML + CSS + 原生 JS，**零后端**
- **禁止**引用任何公有云 CDN / 外部框架（Google Fonts、Bootstrap、SheetJS CDN 等）
- 所有计算在浏览器本地完成，数据不上传
- 必须同时支持：**本地打开**（`file://` 或本地 HTTP）和 **Cloudflare Pages 部署**
- JS 使用普通 `<script src>` 顺序加载，**不用 ES Module**（`type="module"` 在 `file://` 下可能受限）
- xlsx 导出使用 `shared/js/xlsx.js` 内嵌纯 JS 实现，不引入第三方库

## 目录结构

```
/
├── index.html                  # 工具箱首页（索引页）
├── manifest.webmanifest        # PWA 应用清单
├── sw.js                       # Service Worker（离线缓存）
├── _headers                    # Cloudflare 缓存头（sw.js no-cache）
├── icons/
│   ├── clownfish-source.png    # 图标源图（透明底浅蓝小丑鱼）
│   ├── icon-192.png            # PWA / favicon
│   └── icon-512.png
├── scripts/
│   └── generate-icons.py       # 从源图生成 192/512 图标
├── README.md
├── docs/
│   ├── deploy-cloudflare.md    # 部署指南
│   └── framework.md            # 框架说明（人类可读）
├── shared/                     # 跨工具共享
│   ├── css/common.css
│   └── js/
│       ├── utils.js            # 复制、下载、escHtml 等
│       ├── xlsx.js             # xlsx 生成
│       ├── tools-registry.js   # 首页工具分组注册表（publicTools/specialTools）
│       └── pwa.js              # SW 注册（file:// 自动跳过）
└── tools/<tool-id>/            # 各独立工具
    ├── index.html
    ├── css/tool.css            # 工具专属样式（可选）
    └── js/                     # 按职责拆分：逻辑 / 处理 / app
```

## 新增工具流程

1. 在 `tools/<tool-id>/` 创建目录与 `index.html`
2. 引用 `../../shared/css/common.css`，按需引用 shared JS
3. 页头加「← 返回工具箱」链接：`href="../../index.html"`
4. 在 `shared/js/tools-registry.js` 的 `publicTools` 或 `specialTools` 中追加注册条目（`id`、`name`、`description`、`tags`、`path`、`status`）
5. 首页 `index.html` 按“公共工具 / 定制工具”分区从注册表自动渲染，无需改首页逻辑
6. **PWA**：按下方「PWA 约定」在工具页加入 manifest / 图标 / `pwa.js`，并更新 `sw.js` 预缓存列表

## PWA 约定

站点同时支持 **file:// 本地双击** 与 **HTTPS 部署后安装为 Chrome 应用**。PWA 不得破坏 `file://` 可用性。

### 双模式行为

| 场景 | Service Worker | 离线缓存 | 安装为应用 |
|------|----------------|----------|------------|
| `file://` 本地打开 | 不注册（`pwa.js` 检测 `location.protocol === 'file:'` 后跳过） | 无 | 不支持 |
| HTTPS 部署（Cloudflare Pages） | 自动注册根目录 `sw.js` | 有 | 支持 |

### 根目录文件（勿随意移动）

- `manifest.webmanifest`：`start_url` 用 `./`（**不用** `./index.html`，Cloudflare 会 307 重定向）
- `sw.js`：预缓存与 fetch 策略，**必须放站点根目录**
- `_headers`：`/sw.js` 设 `Cache-Control: no-cache`，确保 SW 及时更新
- `shared/js/pwa.js`：各页面 `<body>` 末尾引入；根据脚本路径自动推算 `sw.js` 相对根路径

### 各页面 `<head>` 必须包含（路径按层级调整）

**首页**（`index.html`）示例：

```html
<link rel="manifest" href="manifest.webmanifest">
<meta name="theme-color" content="#2563a8">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="apple-mobile-web-app-title" content="工具箱">
<link rel="icon" href="icons/icon-192.png" type="image/png">
<link rel="apple-touch-icon" href="icons/icon-192.png">
```

**工具页**（`tools/<id>/index.html`）将上述路径改为 `../../manifest.webmanifest`、`../../icons/icon-192.png`；`</body>` 前加：

```html
<script src="../../shared/js/pwa.js"></script>
```

### Service Worker（`sw.js`）约束

1. **预缓存 URL 禁止写 `*/index.html`**：Cloudflare 将 `/tools/foo/index.html` **307 重定向**到 `/tools/foo/`，`cache.addAll` 会因非 200 失败并导致页面异常。须使用目录形式，例如 `./tools/net-policy/`。
2. **安装方式**：用逐项 `fetch` + `cache.put`（`precacheAll`），单个资源失败不影响整体安装；**禁止**对含重定向 URL 使用 `cache.addAll`。
3. **缓存策略**：
   - 页面导航（`request.mode === 'navigate'`）：**网络优先**，离线回退缓存
   - 静态资源（CSS/JS/图标等）：**缓存优先**，后台更新
4. **版本号**：修改预缓存资源后必须递增 `CACHE_VERSION`（如 `webtools-v6` → `webtools-v7`），`activate` 时删除旧缓存。
5. **新增工具时**须将以下资源追加到 `PRECACHE_URLS`：
   - `./tools/<tool-id>/`（目录 URL，非 index.html）
   - 该工具全部 `css/`、`js/` 静态文件
   - 若引用新的 `shared/` 资源，一并加入

### 图标

- 风格：侧视浅蓝小丑鱼，**透明背景**，鱼体尽量占满画布
- 源图：`icons/clownfish-source.png`
- 输出：`icons/icon-192.png`、`icons/icon-512.png`（manifest 与 favicon 共用）
- 重新生成：`pip install pillow && python3 scripts/generate-icons.py`
- 更换图标后：重新生成 PNG，并递增 `sw.js` 的 `CACHE_VERSION`

### manifest 要点

- `display`: `standalone`（全屏应用模式）
- `background_color`: `#f0f2f5`（启动画面底色；图标本身透明）
- `theme_color`: `#2563a8`（与站点 header 主色一致）
- `icons`：192 / 512 各一份，`purpose` 含 `any` 与 `maskable`

### 禁止事项

- 禁止依赖外部 CDN 加载 SW / manifest / 图标
- 禁止在 `file://` 下强制注册 Service Worker
- 禁止将 `sw.js` 放到子目录（注册作用域须覆盖全站）
- 禁止预缓存会 307/404 的 URL

## 网络策略工具（net-policy）约定

- 路径：`tools/net-policy/`
- 支持 IPv4/IPv6 混合输入，独立聚合前缀（IPv4 默认 /24，IPv6 默认 /64）
- 输出模式：按端口分组 / 全部压缩为一行（端口也聚合）
- 地址与端口连接符可自定义；单条最大地址数可拆分
- 核心逻辑：`js/ip.js`（地址）、`js/process.js`（聚合）、`js/app.js`（UI）

## 样式约定

- 公共样式放 `shared/css/common.css`（header、按钮、面板、工具卡片）
- 工具专属样式放 `tools/<id>/css/tool.css`
- 界面文案使用中文；风格简洁，不依赖外部 UI 库

## 代码注释规范

所有 JS 源码须用**中文**注释说明实现逻辑，便于阅读与维护。新增或修改代码时同步更新注释。

### 必须注释的内容

1. **文件头**（每个 `.js` 文件顶部）：模块职责、依赖关系、对外导出对象、典型数据流（输入 → 处理 → 输出）
2. **工具三层结构**（`tools/<id>/js/`）：
   - `process.js` / 核心逻辑文件：解析、算法、合并/转换步骤
   - `app.js`：UI 事件绑定、`init` 流程、调用核心逻辑、渲染结果、复制/导出
   - 其他专用模块（如 `ip.js`、`template.js`）：该层单独承担的算法或数据结构
3. **非显而易见逻辑**：正则含义、位运算目的、合并/覆盖判定规则、降级方案（如剪贴板 fallback）
4. **共享模块**（`shared/js/`）：各导出函数用途及被哪些工具复用

### 注释风格

- 使用 `/** … */` 描述模块与函数；用 `//` 标注行内或步骤说明
- 说明「为什么」和「做什么」，避免复述代码字面意思（如不要写 `// i 加 1`）
- 复杂流程用步骤编号或分段标题，例如 `// 1. 解析输入`、`// 2. 按端口分组`
- 界面文案、用户可见字符串用中文；注释也用中文

### 不必注释的内容

- 自解释的 getter/setter、一行 DOM 操作
- 与文件头已说明内容完全重复的函数（除非该函数有额外边界条件）

### 示例（工具 app.js 文件头）

```javascript
/**
 * 网络策略工具 — UI 交互层
 *
 * 数据流：用户输入文本 → NetPolicyProcess.process() 聚合
 *        → 渲染表格 → lastResult 缓存 → 复制 / xlsx 导出
 *
 * 依赖：BocUtils（复制/下载）、BocXlsx（导出）、NetPolicyProcess、NetPolicyIp
 */
```

## Git 分支与工作流

- **main**：生产主线，仅接收已验证的合并，不直接开发
- **dev**：唯一开发分支，所有功能修改、修复、新工具均**先提交并推送到 dev**

### 日常开发

```bash
git checkout dev
git pull origin dev
# 开发、提交
git push origin dev
```

### 发布到主线（验证通过后）

```bash
git checkout main
git pull origin main
git merge dev
git push origin main
git checkout dev
```

### 禁止事项

- 禁止在 main 上直接开发（除紧急 hotfix，且事后须同步回 dev）
- 禁止创建额外长期功能分支（统一使用 dev）

禁止在 main 上直接开发，禁止创建额外长期功能分支（统一使用 dev）。

## 单元测试与 CI

- **所有工具与共享模块的核心逻辑须有单元测试**（`tests/`，`node tests/run-tests.js`）
- 新增/修改 `process.js` 或 `shared/js/` 算法时同步更新测试
- 注册新工具后更新 `sw.js` PRECACHE_URLS 并递增 CACHE_VERSION
- 发布前执行：`node tests/run-tests.js`、`python3 scripts/check-precache-registry.py`、`python3 scripts/check-compat.py`
- Go 构建已迁至 [webtools-goBuild](https://github.com/cnzhanglu/webtools-goBuild)，本仓库 CI 不跑 go build

## 部署

```bash
git remote set-url origin https://github.com/cnzhanglu/webtools.git
```

Cloudflare：关联 `cnzhanglu/webtools` 仓库，输出目录 `/`。
